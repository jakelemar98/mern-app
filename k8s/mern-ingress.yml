apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: mern-ingress
spec:
  rules:
  - host: path1.jalema01-mern-app.com
    http:
      paths:
      - backend:
          serviceName: mern-app
          servicePort: 8080
  - host: path1.jalema01-mern-app.com
    http:
      paths:
      - backend:
          serviceName: mern-app-back
          servicePort: 5000
      